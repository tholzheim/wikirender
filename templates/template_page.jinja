{#-------------------------------Template Page---------------------------------#}
{#
Generate the template of page for a entity.
Input:
    topic: properties defining the entity
    properties: all properties of the wiki.
Output: Generates the page Template:<topic.name>
#}
{#---------------------------------Import-------------------------------------#}
{% from 'macros/utils.jinja' import render_entity %}
{% from 'macros/template.jinja' import viewmode_tablerow, viewmode_tableheader, blank_template_properties, storemode_subobject, storemode_default, storemode_property, template_arg, viewmode_default, pipe, viewmode_labelfield, template_arg, viewmode_masterdetail %}
{#-------------------------------Template-------------------------------------#}
<noinclude>
This is the template {{ '{{' }}Link|target=Template:{{ topic.name }}|title={{ topic.label }}{{ '}}' }}.
It belongs to the concept/topic {{ '{{' }}Link|target=:Concept:{{ topic.name }}{{ '}}' }}<br>
You may find examples for the use of this template via the {{ '{{' }}Link|target=List of {{ topic.plural_name }}{{ '}}' }}.
=== Usage ===
==== storemode subobject ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|storemode=subobject
{{ '}}' }}
</pre>

==== storemode property ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|storemode=property
{{ '}}' }}
</pre>

==== storemode none ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|storemode=none
{{ '}}' }}
</pre>


==== viewmode tableheader ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|viewmode=tableheader
{{ '}}' }}
</pre>

==== viewmode tablerow ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|viewmode=tablerow
{{ '}}' }}
</pre>

==== viewmode tablefooter ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|viewmode=tablefooter
{{ '}}' }}
</pre>

==== viewmode labelfield ====
<pre>{{ '{{' }}{{ topic.name }}
{{ blank_template_properties(topic.name, properties) -}}
|viewmode=labelfield
{{ '}}' }}
</pre>

=== Source ===
Click on "Edit" to edit the noinclude-part Source of this Template.
[[Category:Template]]

</noinclude>

<includeonly>
{{ '{{' }}#switch: {{ template_arg('storemode') }}
|none=
|subobject= {{ storemode_subobject(topic.name, properties) }}
|#default= {{ storemode_default(topic.name, properties) }}
{{ '}}' }}
{{ '{{' }}#switch: {{ template_arg('viewmode') }}
|hidden=
|masterdetail= {{ viewmode_masterdetail(topic.name, properties) }}
|tableheader= {{ '{' }}{{ viewmode_tableheader(topic.name, properties) }}
|tablerow= {{ viewmode_tablerow(topic.name, properties) }}
|tablefooter={{ pipe() }}{{ '}' }}
|labelfield={{ viewmode_labelfield(topic.name, properties) }}
|#default={{ '{' }}{{ pipe() }} class='wikitable'
! colspan='2' {{ pipe() }}{{ topic.name }}
{{ pipe() }}-
{{ '{{' }}#switch:{{ template_arg('storemode') }} {{ '}}' }}
|property= {{ storemode_property(topic.name, properties) }}
}}
{{ viewmode_default(topic.name, properties) }}
{{ '}}' }}
{{ pipe() }}{{ '}' }}
[[Category:{{ topic.name }}]]{{ '{{' }}#default_form:{{ topic.name }} {{ '}}' }}</includeonly>